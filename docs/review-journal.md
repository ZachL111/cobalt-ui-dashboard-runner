# Review Journal

I treated `cobalt-ui-dashboard-runner` as a project where the smallest useful behavior should still be inspectable.

The local checks classify each case as `ship`, `watch`, or `hold`. That gives the project a small review vocabulary that matches its frontend apps focus without claiming live deployment or external usage.

## Cases

- `baseline`: `view drift`, score 178, lane `ship`
- `stress`: `state pressure`, score 208, lane `ship`
- `edge`: `layout risk`, score 191, lane `ship`
- `recovery`: `interaction cost`, score 229, lane `ship`
- `stale`: `view drift`, score 208, lane `ship`

## Note

This file is intentionally plain so the fixture remains the source of truth.
